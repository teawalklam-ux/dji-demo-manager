# 版本更新记录 (Changelog)

本文件记录 DJI 样机管理系统的所有版本更新。格式参考 [Keep a Changelog](https://keepachangelog.com/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **约定**：页面右下角显示的版本号由 `src/lib/version.ts` 中的 `APP_VERSION` 单一驱动，
> 每次发版必须让它与本文档顶部版本号保持一致（由 `pre-commit` 钩子强制校验）。

---

## [1.53] - 2026-07-25

### 修复 (Fixes)
- **归还提醒收件人调整**：样机归还企业微信提醒不再 @ 销售主管，改为固定真实 @ 林芷因和田潇；正文中的借出申请人仍以普通姓名展示。

---

## [1.52] - 2026-07-25

### 新增 (Features)
- **样机归还企业微信提醒**：样机归还成功后发送企业微信通知，正文标注借出申请人，并通过手机号真实 @ 林芷因及所有启用中的销售主管；重复角色自动去重，同一借用记录只发送一次。

---

## [1.51] - 2026-07-22

### 新增 (Features)
- **我的申请拍照归还**：借用中、部分归还及逾期申请新增明确的“拍照归还”入口，复用现有相机、水印、定位、照片上传和归还 RPC 闭环；多样机申请会显示待归还数量并支持逐台选择。
- **管理员记录清理**：管理员和超级管理员新增“记录清理”页面，仅展示并允许删除借用类型为“测试”或由用户自行取消的申请。

### 安全与完整性 (Security & Integrity)
- **受限原子删除**：数据库再次校验活动管理员身份和记录类型，原子清理关联审批、借用、通知和库存变动；删除仍在借用中的测试数据时安全恢复样机库存。
- **照片延迟清理**：被删除记录的归还照片进入受保护的 Storage 清理队列，由现有定时清理函数分批处理并在失败后重试。
- **取消归属校验**：用户取消申请时显式校验申请人和待审批状态，避免将非本人或已处理申请标记为取消。

### 修复 (Fixes)
- **当日审批即时借出**：当借用日期已到的申请完成最终审批时立即生成借用记录并进入“借用中”，不再因错过每日定时任务而缺少“拍照归还”入口；定时任务继续作为幂等兜底。

---

## [1.40] - 2026-07-22

### 新增 (Features)
- **动态借用类型与专属审批链**：管理员新建审批链时可同步创建借用类型；借用申请自动展示启用中的类型，并按类型精确匹配审批流程。
- **样机列表服务端分页**：列表按每页 50 条加载，支持搜索、分类与派生状态筛选；扫码、批量导入、批量打印及 Excel 导出组件改为按需加载。

### 性能优化 (Performance)
- **仪表盘渐进加载**：统计、逾期样机、库存变动、待审批和最近申请并行查询、独立渲染，避免单个慢查询阻塞整个首页。
- **轻量查询接口**：仪表盘和样机列表使用专用最小字段查询及已上线的 `get_dashboard_summary`、`get_items_page` RPC，降低传输量与浏览器处理开销。

### 修复 (Fixes)
- **企业微信提醒去重**：移除正文中手工拼接的 `@姓名`，统一使用手机号列表触发真实提醒，避免同一用户重复 @；审批通过通知保留指定抄送人。
- **用户邀请安全与兼容性**：仅活动中的超级管理员可邀请、更新或激活账号；修复线上密码校验提示异常，并兼容 Auth 用户邮箱为空的情况。
- **逾期报表实时计算**：报表预览与 Excel 导出按查询当天实时计算逾期天数，不再依赖定时任务历史值。
- **前端稳定性**：统一未知异常提示，稳定通知与样机 Hook 依赖，消除重复加载风险，并补充通用条码和二维码扫描测试。

### 安全 (Security)
- **Supabase 权限收敛**：移除邮箱枚举 RPC，限制数据库函数执行权限，以 invoker API 隔离高权限实现，并补齐关键外键索引。

---

## [1.29] - 2026-07-22

### 新增 (Features)
- **动态借用类型**：新建审批链时可同步创建独立借用类型，借用申请会自动展示启用中的自定义类型并预览对应流程。
- **专属审批链匹配**：自定义类型提交后优先精确匹配专属审批链；数据库拒绝无有效审批链或已停用类型的申请，避免绕过审批。

### 兼容性 (Compatibility)
- **全站类型显示**：申请、审批、归还、报表和 Excel 导出均支持显示管理员创建的借用类型，同时保留客户试用、营销演示和全部兜底流程。

---

## [1.28] - 2026-07-22

### 修复 (Fixes)
- **借用记录逾期天数**：报表预览与 Excel 导出改为按查询当日实时计算，不再依赖每日定时任务写入的历史 `overdue_days`。
- **逾期报表 RPC**：新增遵循 RLS 的只读数据库函数，在查询时派生实时状态与逾期天数，并限制为已登录用户调用。

---

## [1.27] - 2026-07-16

### 性能优化 (Performance)
- **仪表盘首屏**：将样机状态与本月申请的多次串行统计合并为单次 Supabase RPC，复用已加载的用户与角色信息，并让各数据区域独立完成加载。
- **样机列表**：新增带派生预定状态的服务端分页 RPC，列表仅返回展示字段；Excel 导出、扫码、批量导入与条码打印改为按需加载。

### 安全 (Security)
- **数据库权限收敛**：撤销触发器与维护函数的匿名执行权限，关闭用户资料、审批链和客户数据的匿名访问，并合并重复 RLS 策略。
- **高权限 RPC 隔离**：将 8 个 `SECURITY DEFINER` 实现迁移到未暴露的 `private` schema，通过同签名 invoker API 保持前端兼容。
- **账号枚举防护**：移除匿名邮箱存在性检查 RPC，注册流程直接使用 Supabase Auth。
- **外键查询性能**：补齐 9 个外键列索引，改善关联查询与级联操作性能。

### 新增 (Features)
- **首页样机状态分布**：状态分布图新增“预定”样机数量，并从“在库”数量中扣除预定样机，避免重复统计。

---

## [1.26] - 2026-07-16

### 新增 (Features)
- **借用申请 SN 后四位**：选择样机的下拉列表和已选列表显示 `SN ****1234`，并支持按 SN 后四位搜索；选择器查询不再返回完整 SN。
- **样机借用记录扩展**：样机详情的借用记录同时展示已预定与正在借出的记录，并避免同一申请样机重复显示。

---

## [1.25] - 2026-07-15

### 新增 (Features)
- **借用申请样机状态**：选择样机时同步展示在库、预定或借出状态；预定样机标注最近预约区间，借出样机标注当前应归还日期，已选样机列表保持同样提示。

---

## [1.24] - 2026-07-15

### 新增 (Features)
- **样机预定状态**：样机管理列表会将尚未到借出日期、但已有审批通过预约的在库样机标记为“预定”，并支持状态筛选、排序和导出。
- **预约状态安全查询**：新增仅限登录用户调用的预约样机查询函数，并显式撤销匿名执行权限。

### 修复 (Fixes)
- **筛选重置**：修复分类或状态切回“全部”时将 `all` 误作为数据库筛选值的问题。

---

## [1.23] - 2026-07-13

### 修复 (Fixes)
- **企业微信 @ 提醒投递校验**：仅当企业微信 Webhook 明确返回成功时才记录为已发送；失败通知会保留，便于重试和排查。

---

## [1.22] - 2026-07-13

### 修复 (Fixes)
- **客户试用双重审批**：审批人只能处理审批链中当前分配给自己的步骤，管理员不再跳过总经理等后续确认步骤；已纠正 BR-20260713-024，使其重新等待总经理审批。
- **企业微信审批提醒**：审批提醒中的申请人与待审批人名称统一加 `@` 前缀；资料中已填写并绑定企业微信的手机号会同步触发真实群内 @ 提醒。

---

## [1.21] - 2026-07-13

### 新增 (Features)
- **审批流程详情**：审批管理的待审批卡片可打开流程详情，显示当前审批环节、当前审批人及上一级审批意见；上一级意见仅对当前有审批权限的用户开放。

---

## [1.20] - 2026-07-13

### 新增 (Features)
- **多样机预约借用**：一张申请单可选择多台样机；审批通过后按日期预约、到期自动整单出借，并支持逐台归还。
- **日期冲突校验**：借出中的样机仍可申请未来无冲突日期；最终审批会在数据库内原子校验已通过预约、借用与逾期区间，避免重复占用。
- **预约自动化**：新增每日北京时间 00:05 的自动出借任务，并回填历史申请的样机明细与借用记录关联。

---

## [1.19] - 2026-07-13

客户地址簿功能，借用申请时可保存和复用客户信息。

### 新增 (Features)
- **客户地址簿**：借用申请（客户试用类型）时可保存常用客户信息（名称+联系方式），下次直接从地址簿选择快速填充。
  - 新增 `user_customers` 表 + RLS 策略（用户只能读写自己的客户，`super_admin` 可读取所有人的）
  - 借用申请页增加「从地址簿选择」下拉和「保存到地址簿」复选框（默认勾选）
  - 同一用户下客户名称+联系方式唯一，重复保存自动跳过
  - 管理后台新增「客户地址簿」页面（仅 super_admin），可查看 / 搜索 / 删除所有用户的客户信息

---

## [1.18] - 2026-07-13

修复撤销审批时外键约束冲突。

### 修复 (Fixes)
- **撤销审批外键冲突**：修复 `revoke_approval` 函数在删除 `borrow_records` 后插入 `stock_movements` 时，
  因 `borrow_record_id` 外键引用已删除记录导致的约束冲突。将对冲库存变动的 `borrow_record_id` 改为 NULL，
  原借用记录 ID 记入 `notes` 保留审计信息。

---

## [1.17] - 2026-07-13

超级管理员可撤销已通过的审批。

### 新增 (Features)
- **超级管理员撤销审批**：新增 `revoke_approval` RPC 函数（SECURITY DEFINER），仅 super_admin 可调用，
  支持撤销借用中 / 逾期 / 已归还状态的已通过审批。
  - 借用中 / 逾期：恢复样机为在库、删除借用记录、插入对冲库存变动记录（`movement_type='revoke'`）
  - 已归还：保留借用记录和归还照片，仅标记审批撤销
  - 扩展 CHECK 约束：`borrow_requests.status` 增加 `revoked`、`approval_records.action` 增加 `revoked`、
    `stock_movements.movement_type` 增加 `revoke`
  - 扩展 `handle_request_status_changed` 触发器：`revoked` 状态也自动关闭未处理审批记录
  - 前端审批列表页（已审批 Tab）和审批详情页增加撤销入口（仅 super_admin 可见），撤销后自动通知申请人

---

## [1.16] - 2026-07-11

企业微信审批通知改为按审批链顺序逐步推送。

### 新增 (Features)
- **逐步审批通知**：企业微信推送待审批提醒时，"待审批人"只显示审批链中当前步骤对应的审批人（而非所有有权限用户）。
  上一步审批通过后才推送下一步提醒；全部通过后给申请人推送"审批通过"通知；审批拒绝时推送"审批拒绝"通知。
  - `create_borrow_request`：只给第一步审批人发站内通知，不再群发所有 admin/super_admin
  - `process_approval`：审批通过 → 通知下一步审批人；全部通过 → 通知申请人；拒绝 → 通知申请人
  - `notify-approval` v2：去重维度改为 `borrow_request_id + recipient_id`，文案区分待审批/审批通过/审批拒绝
  - 前端审批操作后自动触发企业微信通知

---

## [1.15] - 2026-07-11

本轮集中补齐了用户管理、审批链与扫码体验，并修复了若干导出 / 注册 / 权限问题。

### 新增 (Features)
- **审批链最大借用天数限制**：审批链配置支持设置单次借用的最大天数，超限申请将被拦截。（`94eefcaf`）
- **分类管理与审批链配置对 admin 开放**：admin 角色现在可直接管理分类与审批链，无需 super_admin。（`16b615d6`）
- **注册邮箱预检**：注册前校验邮箱是否已存在，避免重复注册覆盖已有账号。（`cbca76a7`）
- **条码扫描通用识别 + 系统条码快速通道**：扫描器支持通用条码（EAN/UPC/Code39/ITF/CODABAR 等）识别，
  系统条码（DJI-YYYYMMDD-XXXX）走快速通道即时返回，通用条码需连续两次确认防误读。（`e2a024ba`）
- **样机管理表头排序**：条码 / 名称 / 型号 / 分类 / 状态 / 存放位置支持正序 / 倒序排序。（`2439a8f4`）
- **二维码反色支持**：用 `qr-scanner` 替换 `html5-qrcode`，支持反色二维码识别（`setInversionMode('both')`）。（`31efeeb8`）
- **条码标签打印增加存放位置**：打印条码标签时在名称后附带样机存放位置。（`0743bab0`）

### 修复 (Fixes)
- **审批列表重复显示 / 取消申请 RLS / array_lower 错误**：修复审批列表重复条目、取消申请后审批记录未自动关闭，
  以及 `create_borrow_request` 中 `array_lower(v_approver_ids)` 缺维度参数导致的运行时报错。（`8e8d61c7`）
- **邀请用户支持已注册用户的角色更新**：邀请已存在用户时改为更新其角色而非报错。（`94a80db0`）
- **profiles 表 RLS 策略修复**：修复 super_admin 无法查看用户列表的问题。（`de8e0c45`）
- **注册邮箱验证 404 与邀请用户失败**：注册验证邮件跳转地址修正（`emailRedirectTo`），
  邀请改用 Edge Function（`invite-user` + service role）避免登录态冲突。（`9b87888c`）
- **启用原生 BarcodeDetector + 错误态卡片过长修复**：优先使用浏览器原生 `BarcodeDetector`，并修正扫码错误态卡片样式。（`377cf5e2`）
- **混合扫码器修复**：条形码走 `html5-qrcode`、二维码走 `qr-scanner` 的混合模式修复。（`1f3bd848`）
- **借用记录导出字段缺失**：修复导出缺少借用人 / 样机名称 / 样机型号的问题。（`4755d48b`）
- **条码标签重复文本渲染**：移除条码标签中重复的文本渲染，并补充预览 Tailwind 样式。（`f8bcdbf7`）

### 文档 (Docs)
- **README 中英文化**：补全中英双语文档，移除 SQL 迁移中的敏感信息，强化 `.gitignore`。（`d416481c`）
- **修正 README 中文锚点链接**。（`c04c164f`）

---

## [1.14] - 2026-05-31

### 修复 (Fixes)
- 归还照片改为可选：无照片也允许归还（`process_return` 照片参数可选）。（`f31230dd`）
- 新增迁移 `00024`：更新 `process_return` 支持照片参数。（`415699e8`）
- 移除未使用的 `Check` 导入导致构建失败的问题。（`1b086a34`）
- `process_return` 同时支持 `borrow_record_id` 与 `request_id` 查找。（`e0220f4d`）
- 归还流程传入 `borrow_record.id` 而非 `request_id`。（`fda2bb14`）

---

## [1.13] - 2026-05-29

### 修复 (Fixes)
- `process_return` 同时支持 `borrow_record_id` 与 `request_id` 查找路径对齐。（`e0220f4d`）
- 归还流程统一传入 `borrow_record.id`。（`fda2bb14`）

---

## [1.12] - 2026-05-27

### 新增 (Features)
- **归还拍照 + 水印**：后置摄像头现拍，Canvas 将时间戳与 GPS 烧入图片；照片 30 天自动清理，元数据保留 1 年。（`1597a719`）

### 修复 (Fixes)
- 续借 / 归还页面路由参数名统一为 `:id`（修复 `requestId` / `recordId` 不匹配）。（`5b340c78`、`2668afcb`）
- GitHub Pages 子路由刷新 404：新增 SPA 重定向（`2b41d3b2`）与内容哈希强制刷新（`58d6a4e3`）。
- `borrow_records` 查询简化避免 join 超时；super_admin RLS 修复与归还页加载简化。（`fd6a87ed`、`8475fb25`）
- `process_approval` 对 admin / super_admin 自动通过全部步骤，新增「部分通过」Tab。（`2621eacb`）
- `approval_records.approver_id` 改为可空；`create_borrow_request` 找不到指定审批人时回退到 admin / super_admin。（`e8d846c7`）
- `create_borrow_request` 字段名修正（`status` → `is_active`）。（`06463f0a`）

---

## [1.0] - 2026-05-21

初始正式版本，包含完整前端与部署能力（v1.0 – v1.11 的开发成果汇总）。

### 新增 (Features)
- **样机管理**：样机增删改查、分类管理、条码生成与打印。
- **借用流程**：申请、审批队列、审批链、归还、续借、仪表盘快捷操作。
- **用户与权限**：四级角色（super_admin > admin > approver > user）、注册 + 管理员审批、用户管理、super_admin 保护与会转移。
- **扫码能力**：摄像头识别条形码 / 二维码，多种扫描模式（barcode / mixed / qrcode）切换。
- **通知**：站内通知 + 企业微信 Webhook + Realtime 订阅；逾期与审批通知。
- **导出 / 导入**：SheetJS 导出借用记录；Excel / CSV 模板批量导入样机。
- **部署**：GitHub Pages 自动部署（GitHub Actions），移动端响应式适配。

---

[1.15]: #115---2026-07-11
[1.14]: #114---2026-05-31
[1.13]: #113---2026-05-29
[1.12]: #112---2026-05-27
[1.0]: #10---2026-05-21
